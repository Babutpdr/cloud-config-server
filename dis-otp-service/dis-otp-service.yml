env-type: dev
# Spring health monitoring
management:
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: "info,health,refresh"

backbase:
  api:
    override-5xx-error-messages: false
  security:
    public:
      paths: /**
